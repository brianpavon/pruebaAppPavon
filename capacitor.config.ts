import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pruebaAppPavon',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
